// import React, { useState } from "react";
// import classNames from "classnames/bind";
// import styles from "./History.module.scss";

// const cx = classNames.bind(styles);

// function CompositHistory(props) {

//   const handleClick = (action , id) => {
//     props.handleClickParent(action , id)
//   };

//   return (
//     <li className={cx("right__item")} onClick={() => handleClick(props.action , props.idButton)}>
//       <div className={cx("right__img-box", props.isActive ? "active" : "")}>
//         {/* <img src={props.isActive ? props.imageActive : props.imageUnActive}></img> */}
        
//       </div>
//       <p>{props.name}</p>
//     </li>
//   );
// }

// export default CompositHistory;
